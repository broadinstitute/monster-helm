apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-prometheus-metrics-service
spec:
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}
  ports:
    - port: {{ .Values.prometheusMetrics.port }}
      targetPort: http
